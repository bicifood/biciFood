# === CONFIGURACIÓ DE L'APLICACIÓ BICIFOOD ===
spring.application.name=BiciFood Web Application
server.port=8080
server.servlet.context-path=/

# === BASE DE DADES MYSQL (ACTIVAR PER MYSQL LOCAL) ===
# Descomenta aquestes línies per utilitzar MySQL local:
# spring.datasource.url=jdbc:mysql://localhost:3306/bicifood_db?useSSL=false&serverTimezone=UTC
# spring.datasource.username=bicifood_user  
# spring.datasource.password=bicifood_pass123
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.hibernate.ddl-auto=validate

# === JPA/HIBERNATE PER H2 (DESENVOLUPAMENT) ===
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# === LOGGING ===
logging.level.com.bicifood=INFO
logging.level.org.springframework.web=INFO

# === SPRING MVC ===
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# === H2 DATABASE (per desenvolupament sense MySQL) ===
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# === ERROR HANDLING ===
server.error.whitelabel.enabled=false
server.error.include-message=always