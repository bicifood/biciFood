<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- Configuració bàsica de Spring Security -->
    <security:http auto-config="true" use-expressions="true">
        
        <!-- Permetre accés públic a l'API -->
        <security:intercept-url pattern="/api/**" access="permitAll"/>
        <security:intercept-url pattern="/static/**" access="permitAll"/>
        <security:intercept-url pattern="/css/**" access="permitAll"/>
        <security:intercept-url pattern="/js/**" access="permitAll"/>
        <security:intercept-url pattern="/images/**" access="permitAll"/>
        <security:intercept-url pattern="/fonts/**" access="permitAll"/>
        
        <!-- Protegir àrea d'administració -->
        <security:intercept-url pattern="/admin/**" access="hasRole('ADMIN')"/>
        
        <!-- Totes les altres rutes requereixen autenticació -->
        <security:intercept-url pattern="/**" access="permitAll"/>
        
        <!-- Configuració de login -->
        <security:form-login 
            login-page="/login" 
            default-target-url="/dashboard"
            authentication-failure-url="/login?error=true" />
        
        <!-- Configuració de logout -->
        <security:logout 
            logout-success-url="/login?logout=true" 
            invalidate-session="true"
            delete-cookies="JSESSIONID" />
        
        <!-- Desactivar CSRF per API REST -->
        <security:csrf disabled="true"/>
        
    </security:http>

    <!-- Authentication Manager -->
    <security:authentication-manager>
        <security:authentication-provider>
            <!-- Per ara, usuaris en memòria per desenvolupament -->
            <security:user-service>
                <security:user name="admin" password="{noop}admin123" authorities="ROLE_ADMIN"/>
                <security:user name="user" password="{noop}user123" authorities="ROLE_USER"/>
            </security:user-service>
        </security:authentication-provider>
    </security:authentication-manager>

</beans>